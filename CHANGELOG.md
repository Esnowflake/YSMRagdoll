# Changelog / 更新日志

新发布说明从本文件对应版本章节生成。历史记录从原 README 迁移，未推测发布日期。
New release notes are generated from the matching version section. Historical dates are not inferred.

## [Unreleased]

## [0.5.26-beta.1] - 2026-09-18

三平台测试版，保留原有 0.5.26 Forge 正式版；Fabric 完整游戏内验收仍待完成。

### Fixed / 修复

- Fabric 1.21.1 设置页不再在内容绘制后触发原版背景模糊；保留无模糊暗色背景，不影响其他界面或全局设置。

### Changed / 变更

- 新增 Fabric 1.20.1 / 1.21.1 实验性移植，保留 Forge 1.20.1；不构建 Forge 1.21.1 或 NeoForge。
- GitHub 三目标构建矩阵；Fabric 使用真实 YSM 2.6.5 检查捕获、输入及爆炸 Mixin 注入。
- 共享测试迁移到 JUnit 5，零测试执行会导致构建失败；正式游戏内验收仍待完成。
- GitHub 自动构建发布、版本校验、SHA-256 校验文件。
- 草稿发布、预发布后缀、平台命名、构建与发布权限隔离、安全重跑。
- 各产物 Minecraft 兼容范围收紧到其确切目标版本，避免误装到其他版本。
- 中英文 README、独立更新日志和协作文档。
- Windows 本地缓存和临时文件保存在项目盘，读取系统代理。

## [0.5.26]

- 新增“牵引线力矩（0–100）”，默认 70；0 无法牵引，1–99 为有限拉力的柔性牵引，100 为刚性跟随。
- 拉力作用于实际抓取点，由关节传递到身体和四肢；保持原有阻尼与关节角度限制，悬挂时仍能自然下垂、弯曲和摆动。
- 保留关节防脱节保护，修复连续小幅连接修正可能挤入方块的问题。
- 牵引线延伸到主手木棍附近，颜色改为 #33FFCC；依据布娃娃实际滞后弯曲，跟上后逐渐拉直，限制末端翘起与回折。
- 59 项自动测试及完整构建通过；包含 0.5.25 的液体浮力、布娃娃管理和虚空清理等功能。

## [0.5.25]

- 新增“测试”分类和立即切换的“重力枪模式”，默认关闭。
- 木棍按住使用键抓取实际肢体，松开释放，滚轮调距，细蓝色光束指示抓取点。
- 使用有冲量上限的点约束牵引，保留碰撞和重力；抓取期间暂停会抵消牵引的玩家推动及侧翻修正。
- 新增约束牵引、释放下落、旋转抓取点、失效清理和距离范围测试；布局检查覆盖四个分类。
- 新增优化后 0.5.24 的独立回退点。

## [0.5.24]

- 方块扫描在每个 tick 内按位置去重，多具相邻布娃娃复用查询结果。
- 缓存空碰撞形状，状态改变时重新计算；动态形状仍刷新，形状相同时复用刚体。
- 复用扫描临时容器，保留新放置方块的推出保护，并按实际世界坐标扫描碰撞偏移后的区域。
- 新增“布娃娃管理”，显示数量、逐具倒计时、手动清除或永久状态。
- 新增低于世界 Y=-64 的自动清理，不受手动清除和永久模式影响。
- 保留历史版本的回退基线。

## [0.5.23]

- 新增高级设置“创建一个布娃娃”，每次点击独立捕获当前玩家模型并立即初始化物理，仅在本客户端显示。
- 手动创建绕过死亡去重，不发送网络包、不触发玩家死亡、不重放过去的爆炸；保持原有数量与寿命规则。
- 设置页不暂停游戏，连续创建时页面保持打开，显示实际生成结果。
- 易推动默认值改为 90，地面摩擦改为 20；其余默认值按常规/高级设置表。
- 快捷键保持默认未绑定，仅允许绑定后在游戏内按键打开；移除命令和模组列表配置入口，清理残留按键点击。
- 保留上一轮骨骼与刚体对齐修复，补充完整坐标公式、生命周期和维护说明。

## [0.5.22]

- 修复捕获 YSM 模型时临时缓冲复用顶点接收器，触发 `Duplicate delegates` 并导致布娃娃无法生成的问题；
- 捕获缓冲的每次请求返回独立接收器，继续丢弃辅助顶点，不写入游戏的正常渲染缓冲；
- 捕获/重放渲染器首次异常记录完整堆栈；
- 新增原版双接收器、多接收器合并和重复请求的回归测试。

## [0.5.21]

- 设置标签按字体宽度换行，动态计算间距，修复文字与输入框重叠；
- 窄窗口使用可滚动单列，保留窗口缩放前未保存的输入；
- 数量上限调整为 `0-10`，新配置默认 `2`，继续优先淘汰最早的布娃娃；
- 易推动指数 `100` 启用玩家碰撞箱扫掠、穿透分离与速度匹配；
- 新增碰撞响应和中英文界面布局回归测试；
- YSM 模型捕获不稳定的问题仍待后续修复。
- 移除躯干前后倾角的强制扶正，保留关节约束和方块碰撞；新增 100 Hz 密集姿态/FPS 诊断日志，
  用于继续校准初始翻倒姿态。

## [0.5.20]

- 工程和模组名称统一为 `YSM Ragdoll`，中文名为 `YSM布娃娃`；
- 设置界面重构为左侧分类、右侧内容的单页布局；
- 常规分类保留存在时间、手动清除和数量上限；
- 高级分类统一使用 `0-100` 的易推动、地面摩擦和爆炸推动指数；
- 所有数字设置均使用文本输入框，并加入确定、应用和取消语义；
- 新增每 10 秒汇总的 FPS、布娃娃数量和模组渲染耗时日志；
- 模型捕获逻辑保持不变，已知的不稳定加载问题留待后续处理。

## [0.5.19]

- 新增 TNT、苦力怕和兼容原版 Explosion 流程的模组爆炸冲击；
- 新增协议 `2` 的爆炸中心/威力快照，并保留未安装客户端可加入的联机模式；
- 新增高级设置“爆炸冲击指数”，范围 `0-20`，默认 `10`；
- 爆炸按肢体、距离、爆炸威力、质量和方块遮挡施加 JBullet 冲量；
- 加入 1 秒爆炸重放窗口，使被爆炸杀死后生成的布娃娃也能继承该次冲击；
- 移除运行时未实现而导致 `AbstractMethodError` 的 Explosion accessor Mixin；
- 改用 Forge 混淆安全反射与无崩溃后备，并隔离服务端/客户端爆炸异常；
- 将两个指数输入框改为左右两列，修复高级设置文字和输入框重叠；
- 保留 `0.5.16` 已稳定的推动、方块碰撞、关节、地面穿透纠正与姿态回写逻辑。
